version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosec
    - misspell
    - nilerr
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
